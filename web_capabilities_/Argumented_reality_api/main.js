// await navigator.xr.requestSession('immersive-vr'); /// that is when u have a headset

// await navigator.xr.requestSession('immersive-ar'); // u have a headset thaat looks what is in the real environment

// await navigator.xr.requestSession('inline'); // can be used with you phone with camera, or with some cameraic sensors like in apple lidar, or somth like that, it depends on hardvare
